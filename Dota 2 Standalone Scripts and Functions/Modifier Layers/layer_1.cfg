//  Removes keybinds from other kebinding layers
unbindall

//  Binds the key that allowed this layer to become active, so that it changes back to the default layer once released
bind "key" "+keyshift"
