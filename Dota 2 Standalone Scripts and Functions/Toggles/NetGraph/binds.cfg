//  Binds a key to toggle the net_graph on and off
bind "key" "toggle_net_graph"

//  Binds a key to force the net_graph on
bind "key" "show_net_graph"

//  Binds a key to force the net_graph off
bind "key" "hide_net_graph"
